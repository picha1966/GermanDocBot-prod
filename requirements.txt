# GERMAN_DOC_BOT v5.0 - Requirements
# ===================================
# Встановлення: pip install -r requirements.txt

# === Основний двигун бота (aiogram 2.x) ===
aiogram==2.25.1
# HTTP client/server for aiogram + aiohttp web server in bot.py (explicit pin for reproducible installs)
aiohttp>=3.9,<4.0

# === Обробка PDF (Критично для генератора) ===
# pymupdf (fitz) для роботи з існуючими формами та превью
pymupdf>=1.24.0
# reportlab для малювання тексту за координатами
reportlab>=4.0.0

# === Обробка зображень (Для водяних знаків та превью) ===
Pillow>=10.0.0

# === Транслітерація (Кирилиця -> Латиниця) ===
unidecode>=1.3.0

# === RTL / Arabic PDF text (shaping + bidi) ===
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# === Сервер для WebApp анкет ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
python-multipart>=0.0.6

# === Конфігурація та змінні оточення ===
python-dotenv>=1.0.0

# === Платежі (Stripe) ===
stripe>=7.0.0

# === AI Support (handlers/support_ai, utils/support_ai) ===
openai>=1.40.0

# === База даних (Асинхронна робота з SQLite) ===
aiosqlite>=0.19.0

# === Redis (Optional — Termin state persistence, Stage 15) ===
# If not installed or REDIS_URL not set, Termin flow falls back to in-memory.
redis>=5.0.0

# === Email delivery backends (install the one you use) ===
# SendGrid: pip install sendgrid  (set SENDGRID_API_KEY)
sendgrid>=6.11.0
# Resend:   pip install resend    (set RESEND_API_KEY)
# resend>=0.7.0

# =============================================================================
# Dev / test dependencies (not needed in production)
# =============================================================================
pytest>=7.4.0
pytest-asyncio>=0.23.0